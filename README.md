Internal Document Management System (Node + MongoDB)

It allows businesses to manage internal documents with role-based access and per-record ownership enforced directly at the API level.

Key Features: 
>Auth & roles
>Document upload & deletion
>Access control
